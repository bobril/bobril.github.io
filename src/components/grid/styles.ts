import * as b from 'bobril';
import * as colors from '../colors';

export const itemStyle = b.styleDef({
    margin: 40,
    
    display: 'inline-block',
    background: colors.color04
})

export const containerLayout = b.styleDef({
    margin: 'auto',
    width: '100%',
    height: '100%'
})